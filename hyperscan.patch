diff -Nurp a/cmake/Hyperscan.cmake b/cmake/Hyperscan.cmake
--- a/cmake/Hyperscan.cmake	2020-09-30 20:27:23.000000000 +0100
+++ b/cmake/Hyperscan.cmake	2020-12-25 18:06:41.741489769 +0000
@@ -1,9 +1,10 @@
 option (ENABLE_HYPERSCAN    "Enable hyperscan for fast regexp processing [default: OFF]" OFF)
 
 if (ENABLE_HYPERSCAN MATCHES "ON")
-    if (NOT ("${ARCH}" STREQUAL "x86_64" OR "${ARCH}" STREQUAL "i386"))
-        MESSAGE(FATAL_ERROR "Hyperscan is supported only on x86_64/i386 architectures")
-    endif ()
+#    MESSAGE("Checking if hyperscan availible for: ${ARCH}")
+#    if (NOT ("${ARCH}" STREQUAL "x86_64" OR "${ARCH}" STREQUAL "i386" OR "${ARCH}" STREQUAL "aarch64"))
+#        MESSAGE(FATAL_ERROR "Hyperscan is supported only on x86_64/i386 and aarch64 architectures")
+#    endif ()
     ProcessPackage (HYPERSCAN LIBRARY hs INCLUDE hs.h INCLUDE_SUFFIXES
             hs include/hs
             ROOT ${HYPERSCAN_ROOT_DIR} MODULES libhs)
@@ -16,4 +17,4 @@ if (ENABLE_HYPERSCAN MATCHES "ON")
             set (USE_CXX_LINKER 1)
         endif ()
     endif ()
-endif ()
\ No newline at end of file
+endif ()
